# Google AI Studio (Gemini)
GOOGLE_AI_API_KEY=your-key-here
# Model: gemini-2.0-flash-001 or gemini-3-flash-preview
GEMINI_MODEL=gemini-2.0-flash-001

# Pipeline data directory (SQLite DB, logs)
DATA_DIR=/data

# Git repo path (where src/content/articles/ lives)
REPO_DIR=/repo

# Git identity for commits
GIT_AUTHOR_NAME=Areazine Pipeline
GIT_AUTHOR_EMAIL=pipeline@areazine.com

# Optional: GitHub token for pushing (if using HTTPS)
GITHUB_TOKEN=

# Fetch intervals in minutes
FETCH_INTERVAL_CPSC=240
FETCH_INTERVAL_FDA=240
FETCH_INTERVAL_NHTSA=240
FETCH_INTERVAL_NOAA=60

# Publish interval in minutes
PUBLISH_INTERVAL=30

# Max articles per publish batch
MAX_BATCH_SIZE=50
